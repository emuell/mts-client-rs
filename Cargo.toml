[package]
name = "mts-client-rs"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
description = "Safe Rust bindings for the ODDSound MTS-ESP microtuning client"
license = "0BSD"
repository = "https://github.com/emuell/mts-client-rs"
readme = "README.md"
categories = ["multimedia::audio", "api-bindings"]
keywords = ["audio", "midi", "microtuning", "tuning", "mts-esp"]
links = "mtsclient"
exclude = [
    # Prebuilt libMTS binaries and installers
    "vendor/MTS-ESP/libMTS/**",
    # Master-side API, which is not wrapped here.
    "vendor/MTS-ESP/Master/**",
    "vendor/MTS-ESP/.github/**",
]

[build-dependencies]
cc = "1.4"
